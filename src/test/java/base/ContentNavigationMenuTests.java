package base;

import org.openqa.selenium.support.ui.WebDriverWait;
import org.testng.annotations.Test;
import org.testng.Assert;
import pages.ContentNavigationPage;
import pages.Games;

public class ContentNavigationMenuTests extends BaseTests{

/*
    @Test
    public void printContentLinks(){
        ContentNavigationPage page = homePage.clickHamburgerMenuButton();
        System.out.println(page.getContentLinksText());
        Assert.assertEquals(page.getContentLinksText().size(), 6);
    }

    @Test
    public void checkCurrentUrl(){
        ContentNavigationPage page = homePage.clickHamburgerMenuButton();
        Games gamesPage = page.clickGamesLink();
        String currentUrl = gamesPage.getGamesPageUrl();
        Assert.assertEquals(currentUrl, "http://sva-microsite-staging.globaleagle.io/games", "The URL is invalid");
    }
*/



}
